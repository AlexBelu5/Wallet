import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
    alignItems: 'center',
    width: '100%',
    paddingHorizontal: 30,
    paddingTop: 20,
  },
  text: {
    textAlign: 'center',
    marginBottom: 10,
  },
  textInput: {
    minHeight: 50,
    maxHeight: '100%',
    width: '100%',
    borderColor: '#91A6A7',
    borderRadius: 5,
    flexShrink: 1,
    borderWidth: 2,
    marginBottom: 20,
  },
  loading: {
    marginTop: 10,
  },
});
